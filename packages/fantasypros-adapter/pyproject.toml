[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "leaguebrief-fantasypros-adapter"
version = "0.1.0"
description = "LeagueBrief FantasyPros reference CSV adapter"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
include = ["leaguebrief_fantasypros_adapter*"]
